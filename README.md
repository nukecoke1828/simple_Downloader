# simple_Downloader

A simple file downloader supporting HTTP server mode and CLI mode.
This repository contains two implementations on separate branches:

- **main** - HTTP server based downloader
- **cli** - Command-line interface downloader

